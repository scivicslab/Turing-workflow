name: error-handling-workflow
steps:
  - states: ["0", "done"]
    actions:
      - actor: errorActor
        method: riskyOperation

  - states: ["0", "0"]
    actions:
      - actor: errorActor
        method: handleError
