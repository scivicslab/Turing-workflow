name: parallel-execution-workflow
steps:
  - states: ["0", "1"]
    actions:
      - actor: actor1
        method: execute
        arguments:
          task: "task1"
      - actor: actor2
        method: execute
        arguments:
          task: "task2"
      - actor: actor3
        method: execute
        arguments:
          task: "task3"
  - states: ["1", "end"]
    actions:
      - actor: actor1
        method: execute
        arguments:
          task: "finalize"
