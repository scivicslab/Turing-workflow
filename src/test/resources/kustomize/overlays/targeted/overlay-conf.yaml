apiVersion: pojoactor.scivicslab.com/v1
kind: OverlayConf

bases:
  - ../../base

patches:
  - target: main-workflow.yaml
    patch: patch-main.yaml
  - target: setup.yaml
    patch: patch-setup.yaml

vars:
  environment: targeted
